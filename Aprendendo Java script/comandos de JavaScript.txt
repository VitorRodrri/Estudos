                                                                                   no java os numeros são identintificados como number sendos ral ou inteiro
                                                                                   string cadeias de caracteres podendo ser também cpf e numeros de celular 
                                                                                   boolean: true false
-JAVA SCRIPT                                                                         
script
window.alert: cria um alerta na pagina                                             if-> se else-> se não  console.log -> é mesma função que o document.write mas quando eu coloco .js
window.confirm:-----
window.prompt: interage pedindo para escrever algo
  wimdow.alert ('é um prazer te conhecer,' +nome +!) //concatenação
=: receber
var: criar uma variável

+ serve para concatenação e para adição
tem que fazer o java reverter string para numero
number.parseInt(n): número inteiro
number.parsefloat(n): número real com ,
number: o java decide se é inteiro ou real
string(n):
n.toString:
`{}quando for utilizar o ${n} para contas com variável guardada`craze
s.length // quantos caracteres a string tem
s.touppercase() // Tudo para 'MAIÚSCULAS'
S.toLowerCase() // tudo para 'minúscula'

<strong> // faz ficar em negrito
document.write ou writeln // escreve na página. write é escrever!
</br> quebrar para a linha de baixo
n1.toFixed(2) // salvar para fazer alteração
n1.toFixed(2).replace('.', ',') // trocar . por ,
n1.toLocaleString('pt-BR', {style: 'currency', currency: 'BRL'}) //IDENTIFICA como dinheiro brasileiro e podemos mudar para euro ou outros...
 
p1.innerText / HTML
escrever o texto que foi salvo na var

 window.document.write(window.document.URL)
navegando pela árvore do DOM(tudo que é salvo no java)

 var corpo = window.document.body
       corpo.style.background = 'black'
criar uma variavel para mudar o estilo de fundo da página

 p1.style.color = 'blue'
mudar a cor do paragráfo

comandos para criar a interação

 <div id="area" onclick="clicar()">
        Interaja...
    </div>
    ,<script>
        function clicar() {
            var area = window.document.getElementById('area')
            area.innerText = 'Clicou!'